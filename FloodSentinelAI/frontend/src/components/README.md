# Dashboard Components

The production-ready dashboard currently lives in `frontend/js/iot-dashboard-app.js` so FastAPI can serve `/dashboard` immediately without requiring a Node build on this machine.

When Node.js is installed, the Vite project in `frontend/` can be used to continue development with React, TailwindCSS, Framer Motion, Recharts, and Lucide icons.
